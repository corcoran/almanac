name: ci

on:
  push:
    branches: [master]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node
        id: setup-node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Locate pnpm store
        id: pnpm-store
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      # Cache both the pnpm content-addressable store (downloaded tarballs) and
      # every node_modules tree. Caching node_modules skips the link step and,
      # crucially, preserves the compiled better-sqlite3 native addon under
      # node_modules/.pnpm/.../build — avoiding the slow node-gyp rebuild.
      # The key includes the Node version because that native addon is compiled
      # against a Node-major ABI (NODE_MODULE_VERSION); a Node bump must bust the
      # cache or the restored addon fails to self-register under the new runtime.
      - name: Cache pnpm store and node_modules
        id: deps-cache
        uses: actions/cache@v6
        with:
          path: |
            ${{ steps.pnpm-store.outputs.path }}
            node_modules
            packages/*/node_modules
          key: ${{ runner.os }}-node${{ steps.setup-node.outputs.node-version }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-node${{ steps.setup-node.outputs.node-version }}-pnpm-

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm -r typecheck

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm -r test
